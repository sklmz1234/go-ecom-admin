import { Empty } from 'antd';

// 注册（commit 10 实现）：表单校验对齐后端 binding（email 格式、password min=6），
// 注册接口不返 token，成功后跳登录页。
export default function Register() {
  return <Empty description="注册见 commit 10" />;
}
